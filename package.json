{
  "name": "dev-jobs",
  "version": "1.0.0",
  "description": "A job site for developers",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:dev": "nx run-many --output-style stream -t start:dev -p api web",
    "start": "nx run-many --output-style stream -t start -p api web"
  },
  "author": "Sam Trouy",
  "license": "UNLICENSED",
  "devDependencies": {
    "nx": "^18.2.1"
  }
}
